# The supported runtime environment belongs in backend\.env.
# Run .\scripts\windows\Initialize-Development.ps1 to create it from
# backend\.env.windows.example.
#
# This root file remains only as a discovery pointer for tools that expect an
# environment example at repository root.
