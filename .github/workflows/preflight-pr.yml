name: Preflight

on:
  pull_request:

permissions:
  contents: read

jobs:
  go-live-preflight:
    name: Go-Live Preflight (Windows)
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - name: Run Windows go-live preflight
        shell: pwsh
        run: .\infra\go-live-preflight.ps1
