# Observability

The telemetry package exposes operational snapshots for:

- pipeline throughput
- event lag and dead letters
- execution success and rollback rate
- model calibration drift

These are query helpers for the current platform state, not a full metrics exporter.
