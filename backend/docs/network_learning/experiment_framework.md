# Experiment Framework

The experiment framework mirrors persisted strategy experiments into the network layer so experiment evidence can be reused across campaigns.

Each synced record carries:

- base strategy
- variant strategy
- hypothesis
- predicted effect
- actual effect when available
- industry context
