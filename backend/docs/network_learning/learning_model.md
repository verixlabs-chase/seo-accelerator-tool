# Learning Model

The learning model records mutation-level execution outcomes, computes industry similarity, and only allows cross-industry transfer when the similarity matrix clears the configured threshold.

Core persisted data:

- `seo_mutation_outcomes`
- `seo_experiment_results`
- `industry_similarity_matrix`
